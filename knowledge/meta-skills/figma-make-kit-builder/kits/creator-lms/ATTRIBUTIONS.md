# Attributions — creator-lms

## No external component libraries added

This kit uses only:
- React (MIT) — bundled in scaffold
- Native `<video>` element — no third-party video player
- Inline markdown rendering via `white-space: pre-wrap` for Phase 1 demo

## Phase 2 recommendation

Add `react-markdown` (MIT) if rich Markdown rendering is needed in LessonPlayer text blocks.
Add `@radix-ui/react-dropdown-menu` (MIT) if the CurriculumTree context menu needs accessibility.
