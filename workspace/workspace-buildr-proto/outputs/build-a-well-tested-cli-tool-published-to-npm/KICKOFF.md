# Project: build a well-tested CLI tool published to npm

## What you're building

build a well-tested CLI tool published to npm

## Your skills

```yaml
skills:
  - slug: /everything-claude-code:e2e-testing
    role: optional
    reason: >
      [offline] Token overlap score 10/100. Skill tags [e2e, integration, playwright, cypress] matched goal tokens [build, well, tested, cli, tool]. Full causal reasoning requires Groq — set GROQ_API_KEY fo

  - slug: /everything-claude-code:mcp-server-patterns
    role: optional
    reason: >
      [offline] Token overlap score 10/100. Skill tags [mcp, tool-server, agent-tools, protocol] matched goal tokens [build, well, tested, cli, tool]. Full causal reasoning requires Groq — set GROQ_API_KEY 
```

## Execution plan

1. **E2E Testing** (`/everything-claude-code:e2e-testing`) — optional
2. **MCP Server Patterns** (`/everything-claude-code:mcp-server-patterns`) — optional

## Expected output

- A workspace where every included skill has a written causal chain back to a production requirement
- Production cli requirements are all addressed: ergonomic argument parsing with help text; tested with unit and integration tests; published to npm with semantic versioning; documented with README and examples; handles errors gracefully with useful messages; works cross-platform (Node/Bun/Deno if applicable)
- Rejected skills are documented with explicit reasons, not silently excluded

---

*Generated by WorkspaceBuildR — BDI cognitive workspace assembler*
*Model: offline-token-overlap | Offline: true*
