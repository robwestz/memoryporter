# Project: build a production REST API in TypeScript

## What you're building

build a production REST API in TypeScript

## Your skills

```yaml
skills:
  - slug: /everything-claude-code:code-reviewer
    role: supporting
    reason: >
      [offline] Token overlap score 39/100. Skill tags [review, quality, code, standards] matched goal tokens [build, production, rest, api, in]. Full causal reasoning requires Groq — set GROQ_API_KEY for L

  - slug: /everything-claude-code:typescript-reviewer
    role: supporting
    reason: >
      [offline] Token overlap score 39/100. Skill tags [typescript, types, typed, ts] matched goal tokens [build, production, rest, api, in]. Full causal reasoning requires Groq — set GROQ_API_KEY for LLM a

  - slug: /everything-claude-code:backend-patterns
    role: supporting
    reason: >
      [offline] Token overlap score 39/100. Skill tags [backend, server, middleware, di] matched goal tokens [build, production, rest, api, in]. Full causal reasoning requires Groq — set GROQ_API_KEY for LL

  - slug: /everything-claude-code:deployment-patterns
    role: supporting
    reason: >
      [offline] Token overlap score 39/100. Skill tags [deployment, docker, ci, cd] matched goal tokens [build, production, rest, api, in]. Full causal reasoning requires Groq — set GROQ_API_KEY for LLM ana

  - slug: /everything-claude-code:postgres-patterns
    role: supporting
    reason: >
      [offline] Token overlap score 39/100. Skill tags [postgres, postgresql, sql, indexes] matched goal tokens [build, production, rest, api, in]. Full causal reasoning requires Groq — set GROQ_API_KEY for

  - slug: /everything-claude-code:frontend-design
    role: supporting
    reason: >
      [offline] Token overlap score 35/100. Skill tags [frontend, ui, ux, dashboard] matched goal tokens [build, production, rest, api, in]. Full causal reasoning requires Groq — set GROQ_API_KEY for LLM an

  - slug: /everything-claude-code:frontend-patterns
    role: supporting
    reason: >
      [offline] Token overlap score 35/100. Skill tags [react, state, routing, forms] matched goal tokens [build, production, rest, api, in]. Full causal reasoning requires Groq — set GROQ_API_KEY for LLM a

  - slug: /everything-claude-code:python-patterns
    role: supporting
    reason: >
      [offline] Token overlap score 35/100. Skill tags [python, click, async, pydantic] matched goal tokens [build, production, rest, api, in]. Full causal reasoning requires Groq — set GROQ_API_KEY for LLM

  - slug: /everything-claude-code:api-design
    role: optional
    reason: >
      [offline] Token overlap score 13/100. Skill tags [api, rest, openapi, endpoints] matched goal tokens [build, production, rest, api, in]. Full causal reasoning requires Groq — set GROQ_API_KEY for LLM 
```

## Execution plan

1. **Code Reviewer** (`/everything-claude-code:code-reviewer`) — supporting
2. **TypeScript Reviewer** (`/everything-claude-code:typescript-reviewer`) — supporting
3. **Backend Patterns** (`/everything-claude-code:backend-patterns`) — supporting
4. **Deployment Patterns** (`/everything-claude-code:deployment-patterns`) — supporting
5. **Postgres Patterns** (`/everything-claude-code:postgres-patterns`) — supporting
6. **Frontend Design** (`/everything-claude-code:frontend-design`) — supporting
7. **Frontend Patterns** (`/everything-claude-code:frontend-patterns`) — supporting
8. **Python Patterns** (`/everything-claude-code:python-patterns`) — supporting
9. **API Design** (`/everything-claude-code:api-design`) — optional

## Expected output

- A workspace where every included skill has a written causal chain back to a production requirement
- Production api requirements are all addressed: typed request/response contracts; authentication and authorization; input validation and sanitization; error handling with consistent error shapes; rate limiting awareness; OpenAPI or equivalent documentation; tested routes with integration tests; deployable via container or platform
- Rejected skills are documented with explicit reasons, not silently excluded
- Language-specific quality for typescript is enforced by the right specialist skills

---

*Generated by WorkspaceBuildR — BDI cognitive workspace assembler*
*Model: offline-token-overlap | Offline: true*
