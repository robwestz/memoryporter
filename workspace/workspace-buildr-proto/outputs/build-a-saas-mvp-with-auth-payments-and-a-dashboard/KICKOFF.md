# Project: build a SaaS MVP with auth, payments, and a dashboard

## What you're building

build a SaaS MVP with auth, payments, and a dashboard

## Your skills

```yaml
skills:
  - slug: /everything-claude-code:frontend-design
    role: supporting
    reason: >
      [offline] Token overlap score 50/100. Skill tags [frontend, ui, ux, dashboard] matched goal tokens [build, saas, mvp, with, auth]. Full causal reasoning requires Groq — set GROQ_API_KEY for LLM analys

  - slug: /everything-claude-code:security-reviewer
    role: supporting
    reason: >
      [offline] Token overlap score 46/100. Skill tags [security, auth, injection, secrets] matched goal tokens [build, saas, mvp, with, auth]. Full causal reasoning requires Groq — set GROQ_API_KEY for LLM

  - slug: /everything-claude-code:frontend-patterns
    role: supporting
    reason: >
      [offline] Token overlap score 38/100. Skill tags [react, state, routing, forms] matched goal tokens [build, saas, mvp, with, auth]. Full causal reasoning requires Groq — set GROQ_API_KEY for LLM analy

  - slug: /everything-claude-code:api-design
    role: optional
    reason: >
      [offline] Token overlap score 21/100. Skill tags [api, rest, openapi, endpoints] matched goal tokens [build, saas, mvp, with, auth]. Full causal reasoning requires Groq — set GROQ_API_KEY for LLM anal

  - slug: /everything-claude-code:planner
    role: optional
    reason: >
      [offline] Token overlap score 17/100. Skill tags [planning, decomposition, architecture, workflow] matched goal tokens [build, saas, mvp, with, auth]. Full causal reasoning requires Groq — set GROQ_AP

  - slug: /everything-claude-code:typescript-reviewer
    role: optional
    reason: >
      [offline] Token overlap score 13/100. Skill tags [typescript, types, typed, ts] matched goal tokens [build, saas, mvp, with, auth]. Full causal reasoning requires Groq — set GROQ_API_KEY for LLM analy

  - slug: /everything-claude-code:database-reviewer
    role: optional
    reason: >
      [offline] Token overlap score 13/100. Skill tags [database, sql, schema, migrations] matched goal tokens [build, saas, mvp, with, auth]. Full causal reasoning requires Groq — set GROQ_API_KEY for LLM 

  - slug: /everything-claude-code:python-testing
    role: optional
    reason: >
      [offline] Token overlap score 13/100. Skill tags [python, pytest, testing, fixtures] matched goal tokens [build, saas, mvp, with, auth]. Full causal reasoning requires Groq — set GROQ_API_KEY for LLM 

  - slug: /everything-claude-code:documentation-lookup
    role: optional
    reason: >
      [offline] Token overlap score 13/100. Skill tags [docs, documentation, readme, openapi] matched goal tokens [build, saas, mvp, with, auth]. Full causal reasoning requires Groq — set GROQ_API_KEY for L

  - slug: /everything-claude-code:e2e-testing
    role: optional
    reason: >
      [offline] Token overlap score 13/100. Skill tags [e2e, integration, playwright, cypress] matched goal tokens [build, saas, mvp, with, auth]. Full causal reasoning requires Groq — set GROQ_API_KEY for 
```

## Execution plan

1. **Frontend Design** (`/everything-claude-code:frontend-design`) — supporting
2. **Security Reviewer** (`/everything-claude-code:security-reviewer`) — supporting
3. **Frontend Patterns** (`/everything-claude-code:frontend-patterns`) — supporting
4. **API Design** (`/everything-claude-code:api-design`) — optional
5. **Planner** (`/everything-claude-code:planner`) — optional
6. **TypeScript Reviewer** (`/everything-claude-code:typescript-reviewer`) — optional
7. **Database Reviewer** (`/everything-claude-code:database-reviewer`) — optional
8. **Python Testing** (`/everything-claude-code:python-testing`) — optional
9. **Documentation Lookup** (`/everything-claude-code:documentation-lookup`) — optional
10. **E2E Testing** (`/everything-claude-code:e2e-testing`) — optional

## Expected output

- A workspace where every included skill has a written causal chain back to a production requirement
- Production saas requirements are all addressed: multi-tenant data isolation; authentication (session or JWT); payment integration if billing required; dashboard UI with meaningful data display; tested critical paths; deployable with environment config; security hardened (CSRF, XSS, injection); payment processing with webhook handling
- Rejected skills are documented with explicit reasons, not silently excluded

---

*Generated by WorkspaceBuildR — BDI cognitive workspace assembler*
*Model: offline-token-overlap | Offline: true*
