# Case siex-platform-audit

Goal: map SIE-X end-to-end, identify missing core/engine/pipeline files, name the gamechanger gaps, produce a platform blueprint consumers (Bacowr + writer-tool + future verticals) can depend on without fragment-imports

Repo hint: D:/sie_x/

## Workflow

1. Fill the six core artifacts in `core/`.
2. Run `scripts/mechanical_audit.py C:\Users\robin\Downloads\portable-kit\workspace\siex-platform-audit`.
3. Present options and wait for a user choice.
4. Scaffold the chosen branch in `branches/`.
5. Re-run the audit.
