// topbar.js — top bar. Stub. Real impl in Task 17.
export function renderTopbar() {
  document.getElementById("topbar").innerHTML = `<p style="color: var(--text-faint);">topbar stub (Task 17)</p>`;
}
