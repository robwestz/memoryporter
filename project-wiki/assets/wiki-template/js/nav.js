// nav.js — sidebar navigation. Stub. Real impl in Task 16.
export function renderNav() {
  document.getElementById("sidebar").innerHTML = `<p style="color: var(--text-faint);">nav stub (Task 16)</p>`;
}
