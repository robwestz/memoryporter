// pages/search-results.js — search results landing (kept as stub; Cmd+K is the real entry)
export function renderSearchResults(main) {
  main.innerHTML = `<div class="card"><h1>Search</h1><p class="muted">Press <kbd>⌘K</kbd> or <kbd>Ctrl+K</kbd> to open the search palette.</p></div>`;
}
